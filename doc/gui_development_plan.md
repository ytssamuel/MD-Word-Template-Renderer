# MD-Word Template Renderer - GUI 版本規劃

## 專案資訊

**專案名稱：** MD-Word Template Renderer GUI  
**版本：** v2.0  
**規劃日期：** 2025-12-13  
**預計開發時程：** Phase 2 (2-3週)

---

## 一、GUI 版本目標

建立圖形化使用者介面，讓非技術用戶也能輕鬆使用 MD-Word Template Renderer 工具。

### 1.1 目標用戶

- 需要批量產生文件的行政人員
- 不熟悉命令列操作的一般用戶
- 需要快速預覽轉換結果的使用者

### 1.2 核心功能需求

1. **檔案選擇**：透過圖形介面選擇 Markdown 資料和 Word 模板
2. **即時預覽**：預覽解析後的資料結構
3. **單檔轉換**：一鍵完成單一檔案轉換
4. **批次處理**：支援多檔案批量處理
5. **進度顯示**：顯示處理進度和狀態
6. **錯誤提示**：友善的錯誤訊息顯示

---

## 二、技術架構

### 2.1 技術選型評估

| 框架 | 優點 | 缺點 | 評分 |
|------|------|------|------|
| **PyQt6** | 功能完整、跨平台、美觀 | 授權複雜(GPL/商業)、較大 | ⭐⭐⭐⭐ |
| **Tkinter** | 內建、輕量、免授權問題 | 外觀較舊、功能較少 | ⭐⭐⭐ |
| **PySide6** | 類似 PyQt6、LGPL 授權 | 學習曲線、較大 | ⭐⭐⭐⭐ |
| **CustomTkinter** | 現代外觀、基於 Tkinter | 第三方、社群較小 | ⭐⭐⭐⭐ |
| **Dear PyGui** | 高效能、遊戲風格 | 外觀特殊、不適合辦公軟體 | ⭐⭐ |

### 2.2 建議選型：CustomTkinter

**理由：**
1. 基於 Python 內建 Tkinter，無授權問題
2. 提供現代化、美觀的 UI 元件
3. 輕量級，打包後檔案較小
4. 學習曲線平緩
5. 支援深色/淺色主題

**安裝：**
```bash
pip install customtkinter
```

### 2.3 系統架構圖

```
┌─────────────────────────────────────────────────────────┐
│                    GUI 應用層                            │
│  ┌─────────────────────────────────────────────────┐   │
│  │              MainWindow (主視窗)                  │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐ │   │
│  │  │檔案選擇 │ │資料預覽 │ │批次處理 │ │ 設定  │ │   │
│  │  │ Frame   │ │ Frame   │ │ Frame   │ │Frame  │ │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └───────┘ │   │
│  └─────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────┼─────────────────────────────┐
│                    核心業務層                            │
│  ┌───────────────┐  ┌───────────────┐  ┌────────────┐  │
│  │ MarkdownParser│  │ WordRenderer  │  │ Validator  │  │
│  │   (已完成)    │  │   (已完成)    │  │  (已完成)  │  │
│  └───────────────┘  └───────────────┘  └────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 三、介面設計

### 3.1 主視窗佈局

```
┌──────────────────────────────────────────────────────────┐
│ MD-Word Template Renderer                    [─][□][✕]   │
├──────────────────────────────────────────────────────────┤
│ ┌─ 檔案設定 ─────────────────────────────────────────┐   │
│ │                                                      │   │
│ │  Markdown 資料： [________________] [瀏覽...]        │   │
│ │                                                      │   │
│ │  Word 模板：     [________________] [瀏覽...]        │   │
│ │                                                      │   │
│ │  輸出位置：      [________________] [瀏覽...]        │   │
│ │                                                      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ ┌─ 資料預覽 ─────────────────────────────────────────┐   │
│ │                                                      │   │
│ │  系統名稱: 範例系統                                  │   │
│ │  變更單號: CRQ000000100000                           │   │
│ │  預定換版日期: 113年01月15日                         │   │
│ │  ...                                                 │   │
│ │  (共 22 個欄位)                                      │   │
│ │                                                      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ ┌─ 操作 ─────────────────────────────────────────────┐   │
│ │                                                      │   │
│ │  [   驗證資料   ]  [   預覽結果   ]  [   開始轉換  ] │   │
│ │                                                      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ ┌─ 狀態 ─────────────────────────────────────────────┐   │
│ │  ✓ 準備就緒                                          │   │
│ └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
```

### 3.2 批次處理視窗

```
┌──────────────────────────────────────────────────────────┐
│ 批次處理                                      [─][□][✕]   │
├──────────────────────────────────────────────────────────┤
│ ┌─ 來源設定 ─────────────────────────────────────────┐   │
│ │                                                      │   │
│ │  輸入目錄：  [________________] [瀏覽...]            │   │
│ │  檔案篩選：  [*.md          ▼]                       │   │
│ │  Word 模板： [________________] [瀏覽...]            │   │
│ │  輸出目錄：  [________________] [瀏覽...]            │   │
│ │                                                      │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ ┌─ 檔案清單 ─────────────────────────────────────────┐   │
│ │  ☑ sample_01.md                        待處理       │   │
│ │  ☑ sample_02.md                        待處理       │   │
│ │  ☑ sample_03.md                        待處理       │   │
│ │                                                      │   │
│ │  共 3 個檔案                                         │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│ ┌─ 進度 ─────────────────────────────────────────────┐   │
│ │  [████████████░░░░░░░░░░░░░░░░░] 40%  2/5           │   │
│ │  處理中: sample_02.md                                │   │
│ └──────────────────────────────────────────────────────┘   │
│                                                            │
│           [ 全選 ]  [ 取消全選 ]  [   開始批次處理   ]    │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

### 3.3 設定視窗

```
┌──────────────────────────────────────────────────────────┐
│ 設定                                          [─][□][✕]   │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  外觀                                                      │
│  ├─ 主題：      [深色模式 ▼]                              │
│  └─ 縮放比例：  [100%     ▼]                              │
│                                                            │
│  預設路徑                                                  │
│  ├─ 模板目錄：  [________________] [瀏覽...]              │
│  └─ 輸出目錄：  [________________] [瀏覽...]              │
│                                                            │
│  處理選項                                                  │
│  ├─ ☑ 轉換前驗證資料                                      │
│  ├─ ☑ 轉換後自動開啟檔案                                  │
│  └─ ☑ 批次處理時遇錯誤繼續                                │
│                                                            │
│                    [  儲存設定  ]  [  取消  ]              │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 四、功能模組設計

### 4.1 模組結構

```
src/md_word_renderer/
├── gui/                      # GUI 模組（新增）
│   ├── __init__.py
│   ├── main_window.py        # 主視窗
│   ├── batch_window.py       # 批次處理視窗
│   ├── settings_window.py    # 設定視窗
│   ├── preview_widget.py     # 資料預覽元件
│   ├── file_selector.py      # 檔案選擇元件
│   ├── progress_widget.py    # 進度顯示元件
│   ├── theme_manager.py      # 主題管理
│   └── config_manager.py     # 設定管理
├── parser/                   # 解析器（已完成）
├── renderer/                 # 渲染器（已完成）
├── validator/                # 驗證器（已完成）
├── cli/                      # CLI 工具（已完成）
└── __init__.py
```

### 4.2 核心類別設計

```python
# main_window.py
class MainWindow(ctk.CTk):
    """主視窗"""
    
    def __init__(self):
        super().__init__()
        self.title("MD-Word Template Renderer")
        self.geometry("800x600")
        
        self.parser = MarkdownParser()
        self.renderer = WordRenderer()
        self.validator = SchemaValidator()
        
        self._create_widgets()
        self._bind_events()
    
    def _create_widgets(self):
        """建立 UI 元件"""
        pass
    
    def select_markdown_file(self):
        """選擇 Markdown 檔案"""
        pass
    
    def select_template_file(self):
        """選擇模板檔案"""
        pass
    
    def preview_data(self):
        """預覽解析資料"""
        pass
    
    def convert(self):
        """執行轉換"""
        pass
```

### 4.3 事件流程

```
[使用者操作] ──→ [事件處理] ──→ [核心業務] ──→ [UI 更新]
     │               │              │             │
     │               │              │             │
  選擇檔案       驗證路徑       解析/渲染      更新狀態
  點擊轉換       檢查設定       產生輸出      顯示結果
  批次處理       佇列管理       循環處理      更新進度
```

---

## 五、開發計劃

### 5.1 Phase 2 開發時程

| 週次 | 任務 | 預計工時 |
|------|------|----------|
| Week 1 | 基礎框架、主視窗、檔案選擇 | 16h |
| Week 2 | 資料預覽、單檔轉換、錯誤處理 | 16h |
| Week 3 | 批次處理、設定視窗、打包發布 | 16h |

### 5.2 詳細任務清單

#### Week 1：基礎建設

- [ ] 安裝 CustomTkinter 並建立專案結構
- [ ] 實作 MainWindow 基礎框架
- [ ] 實作檔案選擇元件（Markdown、模板、輸出）
- [ ] 實作主題管理（深色/淺色模式）
- [ ] 整合現有核心模組

#### Week 2：核心功能

- [ ] 實作資料預覽功能
- [ ] 實作單檔轉換功能
- [ ] 實作進度顯示
- [ ] 實作錯誤提示對話框
- [ ] 實作轉換後自動開啟功能

#### Week 3：進階功能與發布

- [ ] 實作批次處理視窗
- [ ] 實作設定視窗與設定持久化
- [ ] 實作拖放檔案功能
- [ ] 測試與除錯
- [ ] 使用 PyInstaller 打包為執行檔
- [ ] 撰寫使用說明

### 5.3 里程碑

| 里程碑 | 目標 | 驗收標準 |
|--------|------|----------|
| M1 | 可執行 GUI | 能開啟視窗、選擇檔案 |
| M2 | 單檔轉換 | 能完成單一檔案轉換 |
| M3 | 批次功能 | 能批次處理多檔案 |
| M4 | 發布版本 | 打包為獨立執行檔 |

---

## 六、技術細節

### 6.1 CustomTkinter 基礎範例

```python
import customtkinter as ctk

class App(ctk.CTk):
    def __init__(self):
        super().__init__()
        
        # 視窗設定
        self.title("MD-Word Template Renderer")
        self.geometry("800x600")
        
        # 設定主題
        ctk.set_appearance_mode("dark")  # "dark", "light", "system"
        ctk.set_default_color_theme("blue")
        
        # 建立元件
        self.label = ctk.CTkLabel(self, text="選擇檔案：")
        self.label.pack(pady=10)
        
        self.entry = ctk.CTkEntry(self, width=400)
        self.entry.pack(pady=10)
        
        self.button = ctk.CTkButton(
            self, 
            text="瀏覽...", 
            command=self.browse_file
        )
        self.button.pack(pady=10)
        
        self.convert_btn = ctk.CTkButton(
            self,
            text="開始轉換",
            command=self.convert,
            fg_color="green"
        )
        self.convert_btn.pack(pady=20)
    
    def browse_file(self):
        from tkinter import filedialog
        filename = filedialog.askopenfilename(
            filetypes=[("Markdown 檔案", "*.md")]
        )
        if filename:
            self.entry.delete(0, "end")
            self.entry.insert(0, filename)
    
    def convert(self):
        # 整合核心模組
        pass

if __name__ == "__main__":
    app = App()
    app.mainloop()
```

### 6.2 多線程處理（避免 UI 凍結）

```python
import threading
from queue import Queue

class ConversionWorker:
    """背景轉換工作者"""
    
    def __init__(self, callback):
        self.callback = callback
        self.queue = Queue()
        self.is_running = False
    
    def start(self, tasks):
        """開始處理任務"""
        self.is_running = True
        for task in tasks:
            self.queue.put(task)
        
        thread = threading.Thread(target=self._process)
        thread.daemon = True
        thread.start()
    
    def _process(self):
        """處理佇列中的任務"""
        while self.is_running and not self.queue.empty():
            task = self.queue.get()
            try:
                result = self._convert(task)
                self.callback("success", task, result)
            except Exception as e:
                self.callback("error", task, str(e))
            self.queue.task_done()
        
        self.callback("complete", None, None)
    
    def _convert(self, task):
        """執行單一轉換"""
        parser = MarkdownParser()
        renderer = WordRenderer()
        
        data = parser.parse(task["input"])
        renderer.render_to_file(
            data, 
            task["template"], 
            task["output"]
        )
        return task["output"]
```

### 6.3 設定持久化

```python
import json
from pathlib import Path

class ConfigManager:
    """設定管理器"""
    
    CONFIG_FILE = Path.home() / ".md_word_renderer" / "config.json"
    
    DEFAULT_CONFIG = {
        "theme": "dark",
        "scale": 100,
        "default_template_dir": "",
        "default_output_dir": "",
        "validate_before_convert": True,
        "open_after_convert": True,
        "continue_on_error": True
    }
    
    def __init__(self):
        self.config = self.load()
    
    def load(self):
        """載入設定"""
        if self.CONFIG_FILE.exists():
            with open(self.CONFIG_FILE, "r", encoding="utf-8") as f:
                return {**self.DEFAULT_CONFIG, **json.load(f)}
        return self.DEFAULT_CONFIG.copy()
    
    def save(self):
        """儲存設定"""
        self.CONFIG_FILE.parent.mkdir(parents=True, exist_ok=True)
        with open(self.CONFIG_FILE, "w", encoding="utf-8") as f:
            json.dump(self.config, f, ensure_ascii=False, indent=2)
    
    def get(self, key, default=None):
        return self.config.get(key, default)
    
    def set(self, key, value):
        self.config[key] = value
```

### 6.4 打包為執行檔

```bash
# 安裝 PyInstaller
pip install pyinstaller

# 打包指令
pyinstaller --name="MD-Word-Renderer" \
            --windowed \
            --onefile \
            --icon=resources/icon.ico \
            --add-data="templates;templates" \
            gui_main.py
```

**PyInstaller spec 檔案範例：**

```python
# md_word_renderer.spec
a = Analysis(
    ['gui_main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('templates', 'templates'),
        ('resources', 'resources'),
    ],
    hiddenimports=['customtkinter'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)
exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='MD-Word-Renderer',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='resources/icon.ico',
)
```

---

## 七、依賴清單

### 7.1 新增依賴

```
# requirements-gui.txt
customtkinter>=5.2.0
pillow>=9.0.0        # 圖片處理（圖示）
pyinstaller>=5.0.0   # 打包工具（開發用）
```

### 7.2 完整依賴

```
# requirements.txt (更新)
python-docx>=0.8.11
docxtpl>=0.16.7
Jinja2>=3.1.2
PyYAML>=6.0
jsonschema>=4.17.0
pytest>=7.0.0
customtkinter>=5.2.0
pillow>=9.0.0
```

---

## 八、風險與應對

| 風險 | 可能性 | 影響 | 應對措施 |
|------|--------|------|----------|
| UI 凍結 | 高 | 中 | 使用多線程處理耗時操作 |
| 打包後無法執行 | 中 | 高 | 提早測試打包流程 |
| 跨平台相容性 | 中 | 中 | 在 Windows/Mac 上測試 |
| 字型顯示問題 | 低 | 中 | 使用系統預設字型 |

---

## 九、後續擴充

### 9.1 v2.1 計劃功能

- [ ] 模板編輯器（視覺化編輯 Word 模板）
- [ ] 逆向解析（Word → Markdown）
- [ ] 範本管理（儲存常用設定）
- [ ] 匯出歷史紀錄

### 9.2 v2.2 計劃功能

- [ ] 雲端整合（OneDrive/Google Drive）
- [ ] 排程自動執行
- [ ] API 服務模式

---

## 十、專案檔案結構（Phase 2 完成後）

```
SpeedBOT/
├── src/
│   └── md_word_renderer/
│       ├── __init__.py
│       ├── parser/           # ✅ 已完成
│       ├── renderer/         # ✅ 已完成
│       ├── validator/        # ✅ 已完成
│       ├── cli/              # ✅ 已完成
│       └── gui/              # 🆕 Phase 2
│           ├── __init__.py
│           ├── main_window.py
│           ├── batch_window.py
│           ├── settings_window.py
│           └── widgets/
├── templates/               # ✅ 已完成
├── test/                    # ✅ 已完成
├── doc/
│   ├── user_guide.md        # ✅ 已完成
│   ├── template_syntax_reference.md  # ✅ 已完成
│   ├── implementation_plan.md        # ✅ 已完成
│   └── gui_development_plan.md       # 🆕 本文件
├── resources/               # 🆕 Phase 2
│   ├── icon.ico
│   └── images/
├── md2word.py              # ✅ CLI 入口
├── gui_main.py             # 🆕 GUI 入口
├── requirements.txt
├── requirements-gui.txt    # 🆕 GUI 依賴
└── README.md
```

---

*文件版本：1.0*  
*建立日期：2025-12-13*  
*狀態：準備進入開發階段*
